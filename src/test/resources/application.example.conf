// Please make your own application.conf file with the settings you want
// $ cp application.example.conf application.conf

service {
  baseUrl = ""
  restApiUrl = "http://localhost:8080/todos"
  wsApiUrl = "localhost:8080/ws"
  auth {
    username = "example"
    password = "example"
  }
}
